import { VariantProps, cva } from "cva";

export const {{camelCase name}}Styles = cva(
  "",
  {
    variants: {},
    compoundVariants: [],
    defaultVariants: {},
  },
);

export type {{pascalCase name}}StyleProps = VariantProps<typeof {{camelCase name}}Styles>;
